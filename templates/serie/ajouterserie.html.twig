{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(serieForm) }}
        {{ form_widget(serieForm) }}
        <button>Ajouter</button>
        {{ form_end(serieForm) }}
    </div>
{% endblock %}

